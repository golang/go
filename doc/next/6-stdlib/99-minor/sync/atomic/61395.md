
The new [`atomic.And`](/pkg/sync/atomic#And) and [`atomic.Or`](/pkg/sync/atomic#Or)
operators apply a bitwise `AND` or `OR` to the given input, returning the old value.
It is based on the proposal in [#61395](/issue/61395).
