# Float division does not require a temporary when preparing call arguments.

go tool compile -W p.go
stdout 'DIV float64'
! stdout 'autotmp_.*float64'

-- p.go --
package p

//go:noinline
func sink(i int, f float64) {}

func div(i int, x, y float64) {
	sink(i, x/y)
}
