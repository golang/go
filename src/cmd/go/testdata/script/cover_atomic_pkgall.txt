env GO111MODULE=off

[GOARCH:riscv64] skip 'broken on riscv64: #81175'

go test -coverpkg=all -covermode=atomic x
stdout ok[\s\S]+?coverage

[!race] stop

[GOARCH:loong64] skip 'broken on loong64: #78219'

go test -coverpkg=all -race x
stdout ok[\s\S]+?coverage

-- x/x.go --
package x

import _ "sync/atomic"

func F() {}

-- x/x_test.go --
package x

import "testing"

func TestF(t *testing.T) { F() }
