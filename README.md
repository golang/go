# Why Go?

why-go is a fork of the Go programming language with some niche features that would never make it into the upstream language for one reason or another.

## Features

### [Executor](features/Executor.md)

Executor is a single-threaded Goroutine executor enabling event-loop asynchronous programming, and enabling JS-style async-await using Go's existing primitives.